# Hoist tailwindcss packages to the project's top-level node_modules so that
# Turbopack's module resolver can always find them, even when invoked from a
# parent workspace root directory.
public-hoist-pattern[]=*tailwindcss*
public-hoist-pattern[]=*@tailwindcss*
